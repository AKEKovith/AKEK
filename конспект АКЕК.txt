Акек и Набиев два кента,мы на программирование и нам это нравится!!!
в одной команде можно присвоить значения сразу нескольким переменным. для этого переменных слева от = и такое же количество значений в правой части , разделяя их запятыми.
если значение переменной известно заранее , можно объявить переменную и присвоить ей значение в одной строке.
 var name string = "AKEK"
если значение переменной присваивается одновременно с ее объявлением, тип переменной в объявлении обычно не указывают. Тип значения , присвоенного переменной , будет считаться типом этой переменной.
 var quantity = 0
fmt.Println(reflect.Typeof(quantity))
для строковых переменных нулевым значением является пустая строка , а для переменных bool - значение false.
var myBool bool .
если переменная объявляется без прикоснования значения, то она будет содержать
